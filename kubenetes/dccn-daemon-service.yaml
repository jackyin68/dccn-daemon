apiVersion: v1
kind: Service
metadata:
  name: dccn-daemon-service
  labels:
    name: dccn-daemon-service
spec:
  type: LoadBalancer
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
  selector:
    name: dccn-daemon